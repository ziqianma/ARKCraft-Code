package com.arkcraft.module.blocks.common.handlers;

public class PotionEffectHandler
{

	/*
	@SubscribeEvent
	public void onEntityUpdate(LivingUpdateEvent event) {
		if (event.entity instanceof EntityPlayer){
		EntityPlayer player = (EntityPlayer) event.entity;
		if (event.entityLiving.isPotionActive(ARKCraftPotionEffects.Topor) && !player.capabilities.isCreativeMode) {

			float i =player.getHealth();
			if (i >= 1){
				player.performHurtAnimation();
				player.addExhaustion(10);
				player.addPotionEffect(new PotionEffect(Potion.digSlowdown.id, 10, 2));
			}
			}
		}
	}	*/
}
